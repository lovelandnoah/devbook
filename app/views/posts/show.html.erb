<h1> Posts </h1>
<p><%= @post.content %></p>
<br />
	<%= link_to 'Edit Post', edit_post_path(@post, user_id: @post.user_id) %> | 
	<%= link_to 'Delete Post', post_path(@post), method: :delete, data: { confirm: "Do you really want to delete this post?"} %> |
	<% @comments.each do |comment| %>
		<p><%= "{@user.first_name} #{}"
	<%= link_to 'Comment on Post', new_comment_path(post_id: @post.id, user_id: @user.id) %>